package  {		import flash.display.MovieClip;	import flash.media.Camera;	import flash.media.Video;	import flash.events.Event;	import flash.display.Bitmap;	import FootTracker;	import flash.display.Shape;	import flash.geom.Point;			public class FootTrackerTest extends MovieClip {				var cam:Camera;		var tracker:FootTracker;				var vid:Video;		var circle:Shape;				public function FootTrackerTest() {						cam = Camera.getCamera("0");			cam.setMode(640, 480, stage.frameRate);						tracker = new FootTracker(cam, 640, 480);						vid = new Video(640, 480);			vid.attachCamera(cam);			this.addChild(vid);						circle = new Shape();			this.addChild(circle);						addEventListener(Event.ENTER_FRAME, onEnterFrameHandler);					}				private function onEnterFrameHandler(event : Event) : void {						var point:Point;						if (tracker != null)				point = tracker.track();						circle.graphics.clear();			circle.graphics.lineStyle(5, 0xFF0000);						if (point != null)				circle.graphics.drawCircle(point.x - 5, point.y - 5, 10);					}			}	}